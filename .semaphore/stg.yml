version: "v1.0"
name: Production deployment pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1404

blocks:
  - name: "Deploy"
    task:
      jobs:
      - name: deploy
        commands:
          - doit
