version: "v1.0"
name: Build & Test
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

blocks:
  - name: "Build ppl"
    task:
      jobs:
      - name: language versions
        commands:
          - cache restore elasticsearch-6.2.1
          - cd /tmp
          - wget -nc https://artifacts.elastic.co/downloads/elasticsearch/elasticsearch-6.2.1.deb
          - cache store elasticsearch-6.2.1 elasticsearch-6.2.1.deb


  - name: "Build ppl2"
    task:
      jobs:
      - name: language versions
        commands:
          - cache restore elasticsearch-6.2.1
          - cd /tmp
          - wget -nc https://artifacts.elastic.co/downloads/elasticsearch/elasticsearch-6.2.1.deb
          - cache store elasticsearch-6.2.1 elasticsearch-6.2.1.deb
