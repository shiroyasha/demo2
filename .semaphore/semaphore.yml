version: "v1.0"
name: First pipeline examps
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1404

blocks:
  - name: "Build"
    task:
      jobs:
      - name: language versions
        commands:
          - pwd
          - ls -lah
          - uname -a
